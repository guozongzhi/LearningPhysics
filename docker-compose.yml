version: '3.8'

services:
  db:
    image: pgvector/pgvector:pg16
    container_name: leaningphysics_db
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=leaningphysics
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
